## 2024-05-15 - Empty State Elements
**Learning:** Using `<p>` tags for empty states might fail contrast checks if they don't inherit the global text color properly.
**Action:** Prefer using `<div>` or `<span>` for plain text elements like empty states without custom CSS to ensure proper contrast inheritance.
