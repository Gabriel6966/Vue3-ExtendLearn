## 2026-05-31 - [Wrap Checkboxes in Labels]
**Learning:** Wrapping native checkboxes and their descriptive text inside a `<label>` tag significantly increases the clickable hit area, improving accessibility and usability for all users.
**Action:** Always wrap checkboxes and their labels in a `<label>` tag or use `for`/`id` attributes to explicitly associate them.
