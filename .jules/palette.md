## 2026-05-14 - Vue v-model missing ARIA labels
**Learning:** Native HTML form inputs bound to Vue's `v-model` do not automatically generate accessible labels in this app's components.
**Action:** Always provide explicit accessible labels (e.g., `aria-label`) for native HTML form inputs in Vue components.
