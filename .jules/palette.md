## 2026-09-01 - Todo List Empty State and Accessibility
**Learning:** Screen readers lack context on form inputs without labels, and users can be confused when lists are empty without visual feedback.
**Action:** Always ensure all form inputs have an `aria-label` and add an empty state to lists.
